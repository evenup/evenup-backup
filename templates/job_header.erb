# encoding: utf-8

Backup::Model.new(:<%= scope.lookupvar('name') %>, "host backup") do
  ##
  # Split [Splitter]
  #
  # Split the backup file in to chunks of 2G
  #
  split_into_chunks_of 2048
  
  encrypt_with OpenSSL

